import { start } from "wrst";
import App from "./App.tsx";

// The bundle's entry point - import the framework, import your root component,
// and start. The dev server / build bundles this file.
start(App);
